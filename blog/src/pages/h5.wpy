<style>
.content {
  display: flex;
  flex-direction: column;
  background-color: #CCEEFF;
}
.small_content {
  display: flex;
  flex-direction: column;
  align-items: center;
  background-color: #FFFFFF;
  /* border: 1px solid #FFFFFF;
  border-radius: 0.8rem; */
  height: 10rem;
  margin: 0.5rem 0.8rem 0.2rem 0.8rem;
  box-shadow: 2px 2px 3px #888888;
  position: relative;
}
.line {
  background-color: #000000;
  height: 0.5px;
  width: 90%;
}
.label {
  position: absolute;
  bottom: 0.5rem;
  /* padding-bottom: 0.8rem; */
  width: 100%;
  display: flex;
  flex-direction: row;
  justify-content: center;
  font-size: 13px;
}
</style>

<template>
  <view>
    <view class="content">
      <view class="small_content" wx:for="{{h5}}" wx:key="index">
        <view>{{item.title}}</view>
        <view class="line"></view>
        <view class="line"></view>
        <view class="label">
          <view>{{item.type}}</view>
          <view>{{item.column_line}}</view>
          <view>{{item.data}}</view>
        </view>
      </view>
    </view>
  </view>
</template>

<script>
  import h5 from '../content/h5.js'
  import wepy from 'wepy'
  export default class H5 extends wepy.page {
    config = {
      navigationBarTitleText: 'H5'
    }
    data = {
      h5: []
    }
    methods = {
    }
    onLoad() {
      let self = this
      self.h5 = h5
      wepy.getSystemInfo({
        success(res) {
          self.height = res.windowHeight
          self.width = res.windowWidth
        }
      })
    }
  }
</script>
