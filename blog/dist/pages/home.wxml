
  <view class="view">
    <view bindtap="js"  wx:for="{{a}}" wx:key="index" data-index="{{index}}">
      <view class="type">
        <view>
          <image class="image" src="{{item.image}}"/>
        </view>
        <view class="time">
          <view style="font-size: 20px">{{item.type}}</view>
        </view>
      </view>
    </view>
  </view>