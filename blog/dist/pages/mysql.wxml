
  <view>
    <view class="content">
      <view class="small_content"  wx:for="{{mysql}}" wx:key="index">
        <view>{{item.title}}</view>
        <view class="line"/>
        <view class="line"/>
        <view class="label">
          <view>{{item.type}}</view>
          <view>{{item.column_line}}</view>
          <view>{{item.data}}</view>
        </view>
        </view>
      </view>
    </view>